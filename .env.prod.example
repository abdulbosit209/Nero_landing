# Copy to .env on the deploy host (e.g. /opt/nero_landing/.env) and fill in real
# values. Read by docker-compose.prod.yml. Never commit the filled-in .env file.
COOKIE_VALIDATION_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
YANDEX_MAPS_API_KEY=
